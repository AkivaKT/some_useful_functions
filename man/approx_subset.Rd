% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_approx_subset.R
\name{approx_subset}
\alias{approx_subset}
\title{Find an approximate subset of a dataframe based on length and sum criteria.}
\usage{
approx_subset(
  input_df,
  column_name,
  target_length,
  target_sum,
  sum_tolerance = 0.1,
  max_iterations = 1000,
  seed = NULL
)
}
\arguments{
\item{input_df}{The input dataframe from which to find the subset.}

\item{column_name}{The name of the column in the dataframe to be evaluated.}

\item{target_length}{The target length for the subset.}

\item{target_sum}{The target sum for the subset.}

\item{sum_tolerance}{The tolerance allowed for the difference between the subset sum and target sum.}

\item{max_iterations}{The maximum number of iterations for the search.}

\item{seed}{An optional seed for reproducibility.}
}
\value{
A dataframe subset that meets the length and sum criteria based on the specified column.
}
\description{
This function searches for a subset of a given dataframe that approximately matches
both the specified target length and target sum within provided tolerances.
}
\examples{
# Generate sample dataframe
input_df <- data.frame(values = c(2, 5, 8, 3, 10, 6, 1, 4, 7))
column_name <- "values"
target_length <- 3
target_sum <- 16
sum_tolerance <- 0.2
max_iterations <- 1000

# Find the approximate subset
subset_result <- approx_subset(input_df, column_name, target_length, target_sum, sum_tolerance, max_iterations)

# Print the result
print("Approximate subset:")
print(subset_result)

}
